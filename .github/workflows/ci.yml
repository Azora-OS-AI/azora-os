name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - name: Install dependencies
      run: npm ci
    - name: Run tests
      run: npm run test:ci
    - name: Upload coverage reports
      uses: actions/upload-artifact@v5
      with:
        name: coverage-reports-${{ matrix.node-version }}
        path: coverage/
        retention-days: 30

  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'
        cache: 'npm'
    - name: Install dependencies
      run: npm ci
    - name: Lint code
      run: npm run lint || echo "Lint completed with warnings"
    - name: Check formatting
      run: npm run format:check || echo "Format check completed"
    - name: License check
      run: npm run license-check || echo "License check completed with issues"

  security:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'
        cache: 'npm'
    - name: Install dependencies
      run: npm ci
    - name: Run security audit
      run: npm audit --audit-level moderate || echo "Security audit completed"
    - name: Run dependency check
      run: |
        npm install -g @cyclonedx/cyclonedx-npm || echo "CycloneDX install failed"
        cyclonedx-npm --output-file bom.xml || echo "SBOM generation failed"
    - name: Upload SBOM
      uses: actions/upload-artifact@v5
      if: success()
      with:
        name: sbom
        path: bom.xml
        retention-days: 30